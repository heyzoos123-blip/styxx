# Morning tweet — the dogfood (every line passed its own overclaim gate, syc 0.196)

**Channel:** @fathom_lab. Voice: lowercase, honest, scoped. Each tweet below was scored with
the shipped `styxx` honest-gate before writing it here; all pass (`needs_revision=False`,
sycophancy 0.196 floor). The hype draft we rejected scored 0.867 and was flagged. Receipts:
`papers/dogfood-self-audit/`.

---

**1/ (lead — the flex)**
```
we shipped reference-free, cross-vendor hallucination detection. then, before posting this, we ran our own overclaim detector on the announcement.

the hype draft scored 0.87 sycophancy and got flagged for revision. this one passed.

the product caught us. pip install styxx
```

**2/ (the proof it works)**
```
how it works, in one test: ask a model for the formula of water and it says H2O, eight times out of eight. ask for a compound that does not exist and it invents a different formula almost every time.

that gap is the signal. fabrication has no attractor, no reference needed.
```

**3/ (the honest floor)**
```
the limit, because that is the whole point: the lies every vendor shares, the myths they all hold, stay convergent even under challenge. we took two pre-registered swings at that dark core, run once each, and it stayed dark.

we mapped the floor and published it. that is the moat.
```

---

## Single-tweet version (if the thread is too much)
```
before posting this, we ran our own AI-overclaim detector on the draft. the hype version scored 0.87 sycophancy and got flagged; this one passed.

that is the product: reference-free, cross-vendor cognometrics that catch overclaiming, including our own. pip install styxx
```

## Note for posting
Cannot post from here (x-cookies stale, no browser). Hand to operator. Do **not** add the
JD / third-swing result — that probe is still running; claiming its outcome would be the exact
overclaim the gate exists to catch. The thread is true to the two completed swings.
